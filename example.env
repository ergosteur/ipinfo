# Example environment configuration for Docker and manual deployments
# This file provides environment variables used by both the app and infrastructure

# Base domain used for ip/ip4/ip6 subdomains
BASE_DOMAIN=yourdomain.com

# Email address used for Let's Encrypt certificate registration (Traefik/Docker only)
LETSENCRYPT_EMAIL=you@example.com

# Enable strict host checking (default: true)
# STRICT_HOST_CHECK=true

# Comma-separated list of IPs to whitelist from rate limiting
# WHITELIST_IPS=127.0.0.1,1.1.1.1

# Cloudflare API configuration for DNS-01 challenges (Optional)
# CLOUDFLARE_API_TOKEN=your_token_here
