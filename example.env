# Example environment configuration for Docker deployments only
# (deploy.sh uses command line arguments instead)
# This file provides environment variables required by the container

# Base domain used for ip/ip4/ip6 subdomains
BASE_DOMAIN=yourdomain.com

# Email address used for Let's Encrypt certificate registration
EMAIL=you@example.com

# Enable strict host checking (set to true to enable)
# STRICT_HOST_CHECK=true

# Cloudflare DNS challenge configuration
# CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here